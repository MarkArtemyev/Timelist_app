<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todotimerapp2">

    <!-- Разрешения ДОЛЖНЫ быть здесь, на уровне <manifest> -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TodoTimer"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoTimerApp2">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TodoTimerApp2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ресивер, который показывает уведомление, когда таймер истёк -->
        <receiver
            android:name=".timer.TimerAlarmReceiver"
            android:exported="false" />
    </application>
</manifest>
